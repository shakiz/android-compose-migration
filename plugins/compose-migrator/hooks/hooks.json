{
  "_comment": "Optional quality hooks for compose-migrator. Both scripts no-op (exit 0) when their tooling is absent, so this never breaks a project. To DISABLE, remove this file reference from plugin.json or delete this file.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/kotlin-format.sh\""
          },
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-imports.sh\""
          }
        ]
      }
    ]
  }
}
